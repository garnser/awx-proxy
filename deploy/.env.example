# Build metadata (used by compose + Makefile)
VERSION=0.1.0
GIT_SHA=dev
BUILD_DATE=dev

# Database
DATABASE_URL=postgresql+psycopg2://awx_proxy:awx_proxy@postgres:5432/awx_proxy

# AWX
AWX_BASE_URL=https://awx.internal
AWX_TOKEN=changeme

# Auth / OAuth
OAUTH_ISSUER=https://issuer.example
OAUTH_AUDIENCE=awx-proxy
